@model IEnumerable<Rule.BL.Models.FoundationsDTO>

<h2>Foundations</h2>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Description</th>
            <th>Link</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Link</td>
            </tr>
        }
    </tbody>
</table>

<form asp-controller="Foundations" asp-action="Index" method="post">
    <button type="submit" class="btn btn-danger">Return</button>
</form>